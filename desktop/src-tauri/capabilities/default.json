{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "RAI OS desktop capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "shell:allow-open"
  ]
}
