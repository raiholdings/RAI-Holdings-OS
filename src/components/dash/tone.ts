import type { Tone } from "@/lib/dashboard";

export const toneVar: Record<Tone, string> = {
  holdings: "var(--color-holdings)",
  fund: "var(--color-fund)",
  lab: "var(--color-lab)",
  one: "var(--color-one)",
  ok: "var(--color-ok)",
  warn: "var(--color-warn)",
};
